engine-strict=true
# Build sharp native modules for Linux x64 to prevent "Cannot find module 'sharp-linux-x64.node'" error
build-from-source=true
# Use environment variable style for platform configuration
# Note: These are legacy and won't work - use environment variables in scripts instead
python=python3
