engine-strict=true
# Build sharp native modules for Linux x64 to prevent "Cannot find module 'sharp-linux-x64.node'" error
build-from-source=false
# Prefer prebuilt binaries when available
python=python3
# Platform-specific configuration for native modules
platform=linux
arch=x64
